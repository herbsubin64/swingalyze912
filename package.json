{
  "name": "swingalyze-coach",
  "version": "2.8.0",
  "private": true,
  "description": "Swingalyze Coach v2.8.0 — Python analyzer adapter (real tempo) + fallback; same UI.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "PORT=3001 node server.js"
  },
  "engines": { "node": ">=18" }
}
